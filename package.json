{
  "name": "wardrobe-app",
  "private": true,
  "version": "2.0.0",
  "description": "Luxury boutique clienteling journal",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "icons": "python3 scripts/generate-app-icons.py",
    "cap:sync": "CAPACITOR=1 npm run build && npx cap sync ios",
    "cap:open:ios": "npx cap open ios",
    "cap:ios": "npm run cap:sync && npx cap open ios"
  },
  "dependencies": {
    "@capacitor/app": "^8.0.0",
    "@capacitor/contacts": "^1.0.1",
    "@capacitor/core": "^8.0.0",
    "@capacitor/filesystem": "^8.0.0",
    "@capacitor/haptics": "^8.0.0",
    "@capacitor/ios": "^8.0.0",
    "@capacitor/share": "^8.0.0",
    "@capacitor/splash-screen": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0",
    "vite": "^6.2.0"
  }
}
